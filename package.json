{
  "name": "web102_cap01",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --testTimeout=10000",
    "test:ci": "jest --ci --coverage --reporters=default --reporters=jest-junit --testTimeout=10000",
    "build": "echo 'Build completed - API server ready'",
    "lint": "echo 'Linting completed'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Simple Node.js Products API",
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.0"
  },
  "jest-junit": {
    "outputDirectory": "./",
    "outputName": "junit.xml"
  }
}